- defaultTab: nodes
  description: |-
        <Add specific URL or path of the pipeline in github>
        sudo docker run --rm --net=host \
                    -v /home/bnbiuser/secrets/dw_db:/app/dw-secret \
                    -e APP_DW_SECRET=/app/dw-secret \
                    containers.mpi-internal.com/yapo/incremental-buyers:latest
  executionEnabled: true
  group: Core
  loglevel: DEBUG
  name: Core - Buyers - User buyers and sellers 
  nodeFilterEditable: false
  nodefilters:
    dispatch:
      excludePrecedence: true
      keepgoing: false
      rankOrder: ascending
      successOnEmptyNodeFilter: false
      threadcount: '1'
    filter: 'name: dockerhost'
  nodesSelectedByDefault: true
  notification:
    onfailure:
      email:
        recipients: data_team@adevinta.com
        subject: ${job.execid} ${execution.status} ${job.name} - Fallo definitivo
      plugin:
        configuration:
          slack_channel: ''
          webhook_base_url: https://hooks.slack.com/services
          webhook_token: T017F9KHA1Y/B01BL7C1CSY/Ai9NzdCrBUA5Ru5sa8JHYrjR
        type: SlackNotification
  notifyAvgDurationThreshold: null
  schedule:
    month: '*'
    time:
      hour: '03'
      minute: '0'
      seconds: '0'
    weekday:
      day: '*'
    year: '*'
  scheduleEnabled: true
  sequence:
    commands:
    - script: |-
        sudo docker pull containers.mpi-internal.com/yapo/incremental-buyers:latest
        sudo docker run --rm --net=host \
                        -v /home/bnbiuser/secrets/dw_db:/app/dw-secret \
                        -e APP_DW_SECRET=/app/dw-secret \
                        containers.mpi-internal.com/yapo/incremental-buyers:latest
    keepgoing: false
    strategy: node-first
  timeZone: America/Santiago
